import {
	getAllNewsGroupsAsArray,
	getNewsGroupByKey,
	getNewsFeedsByGroup,
	getNewsFeedBySlug
} from './FeedsManager';

const assert = require('assert');

describe('Feeds Manager Tester', function() {
  it('ensure getAllNewsGroupsAsArray returns array keys', () => {
    // assert( expression === expected );
	});
	
	it('ensure getNewsGroupByKey returns an element by array key', () => {
    // assert( expression === expected );
	});

	it('ensure getNewsFeedsByGroup returns an element by array key', () => {
    // assert( expression === expected );
	});

	it('ensure getNewsFeedBySlug returns an element by array key', () => {
    // assert( expression === expected );
  });
});
